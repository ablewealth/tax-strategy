# Environment Variables for Tax Strategy Optimizer

# Backend URL for API calls (default is localhost for development)
REACT_APP_BACKEND_URL=http://localhost:3001

# Note: The Gemini API key is now securely stored in the backend .env file
# and no longer needed in the frontend